# Template Handoff

Scope: This challenge is self-contained in this folder.

Build Target:
1. Create a 50MB disk image that contains deleted and non-deleted files
2. Place the flag in a fresh file called secrets.txt
3. Delete secrets.txt from disk image but ensure that it is still recoverable
4. Make sure the flag is recoverable through strings, autopsy, or foremost