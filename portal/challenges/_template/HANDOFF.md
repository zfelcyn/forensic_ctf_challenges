# Template Handoff

Scope: This challenge is self-contained in this folder.

Tasks:
1. Implement or replace the files in `assets/`.
2. Keep the metadata in `challenge.json` aligned with the actual challenge.
3. Write or update the internal solution in `solution/`.
4. Add Docker files under `deploy/` only if this specific challenge needs a running service.
