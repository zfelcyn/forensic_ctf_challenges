# Template Handoff

Scope: This challenge is self-contained in this folder.

Tasks:
1. Implement or replace `generate.py` so it writes the solver bundle to the provided output directory.
2. Keep the metadata in `challenge.json` aligned with the actual challenge.
3. Add or update any source assets in `assets/`.
4. Write or update the internal solution in `solution/`.
5. Add Docker files under `deploy/` only if this specific challenge needs a running service.
