# Mailbox Heist Handoff

Scope: Implement only this folder.

Build target:
1. Generate a connected set of 4:23 AM `.eml` artifacts tied to the same Claude Bot incident.
2. Split the recovery clues across headers, an HTML link, a compressed attachment, and a sealed archive.
3. Require a solver to combine the clues and decrypt the final memo to recover the flag.
4. Keep the solve path deterministic and solvable with common command-line tools.

Keep unchanged:
- Portal-wide schema
- Other challenge directories
